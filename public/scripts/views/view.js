'use strict';

var app = app || {};

(function(module){
  var view = {};

  view.menuToggle = () => {
    var $menu = $('#menu');
    var $hamburger = $('.icon-menu');
    var $cross = $('.icon-cross');

    $hamburger.on('click', function(e){
      
    });
  };

  view.module = view;
})(app);
